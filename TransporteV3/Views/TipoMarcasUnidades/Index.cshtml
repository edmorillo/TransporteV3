@model IEnumerable<TransporteV3.Entidades.TipoMarcasUnidade>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoMarcaUnidad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoMarcaUnidad)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoMarcaUnidad">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoMarcaUnidad">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoMarcaUnidad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
