@model TransporteV3.Entidades.TiposDocumento

@{
    ViewData["Title"] = "Delete";
}

<div class="container-fluid px-4">

    <div class="card-header">
        <h2 class="mt-4">Eliminar tipo de documento</h2>
        <ol class="breadcrumb mb-4">
            <li class="breadcrumb-item"><a class="nav-link btn btn-link" asp-controller="Home" asp-action="Index">Inicio</a></li>
            <li class="breadcrumb-item active">Eliminar tipo de documento</li>
        </ol>
    </div>
    <div class="card mb-4">


        <div class="card-body">
            <h3>¿Estás seguro que quieres eliminar esto?</h3>
            <br />
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.TipoDocumento)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.TipoDocumento)
                </dd>
            </dl>

            <form asp-action="Delete">
                <input type="hidden" asp-for="IdTiposDocumentos" />
                <input type="submit" value="Eliminar" class="btn btn-danger" />
                <a asp-action="Index" class="btn btn-light">Cancelar</a>
            </form>
        </div>
    </div>
</div>
