@model IEnumerable<TransporteV3.Entidades.TipoUnidade>

@{
    ViewData["Title"] = "Index";
}

<div class="container-fluid px-4">

    <div class="card-header">
        <h2 class="mt-4">Administrar tipos de unidades</h2>
        <ol class="breadcrumb mb-4">
            <li class="breadcrumb-item"><a class="nav-link btn btn-link" asp-controller="Home" asp-action="Index">Inicio</a></li>
            <li class="breadcrumb-item active">Administrar tipos de unidades</li>
        </ol>
    </div>
    <div class="card mb-4">


        <div class="card-body">
            <p>
                <a asp-action="Create" class="btn btn-primary rounded-pill px-3">Crear tipo de unidades</a>
            </p>
            <table id="datatablesSimple">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Detalle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTipoMarcaUnidadesNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Detalle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTipoMarcaUnidadesNavigation.TipoMarcaUnidad)
            </td>
                <td>
                    <a asp-action="Edit" class="btn btn-info" asp-route-id="@item.IdTipoUnidad"><i class="bi bi-journal-text"></i></a>
                    @*@if (User.IsInRole(Constantes.RolAdmin))
                {
                <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.IdEstado"><i class="bi bi-trash3"></i></a>
                }*@
                </td>
            </tr>
        }
    </tbody>
</table>
        </div>
    </div>
</div>
